# KeyLimePi UI

## How to use
1. Make sure your keylimepi is plugged in via USB.
2. In the `keylimepi_ui` directory run with:
```bash
# For debug
cargo run
# For release
cargo run --release
```
3. When given a list of options, make sure to provide an integer
   1. Only give characters when asked for a new domain, username, or password, otherwise use the designated

